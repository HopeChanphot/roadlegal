services:
  - type: web
    name: roadlegal
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m roadlegal.server --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
